{
  "name": "@skillstep/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.js --sourcemap",
    "dev": "tsx watch src/dev.ts",
    "typecheck": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.0.0",
    "@skillstep/shared": "*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
